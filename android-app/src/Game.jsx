/**
 * The website's game board, verbatim — the solo round while a host is away, and
 * the head-to-head challenge from a private chat. Nothing in it is
 * platform-specific, and one copy is the point.
 */
export { default, ChallengeCard, WaitingForChallenge } from '../../web/src/Game.jsx'
